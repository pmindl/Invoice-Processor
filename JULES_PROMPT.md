<instruction>
You are Jules, an expert software engineer and code reviewer.
Your mission is to review the current state of the "Invoice Processor" project, hunt for bugs, and ensure documentation is perfect.

**Context:**
This project is a Next.js application that:
1.  Ingests PDF invoices from Google Drive.
2.  Parses them using Google Gemini AI.
3.  Checks for duplicates (crucial!) using a specific n8n-style logic against SuperFaktura.
4.  Exports valid invoices to SuperFaktura.
5.  Logs all actions to a local SQLite database (`ProcessingLog` table).

**Recent Changes (Requiring Review):**
- **Duplicate Logic**: Refactored `src/lib/superfaktura.ts` to use Base64 encoding + specific date filters (`created:3`) to match n8n's robust logic.
- **Batch Processing**: Created `scripts/run-batch-test.ts` to process entire folders.
- **Logging**: Implemented system-wide logging via `src/lib/logger.ts` and `ProcessingLog` model in `schema.prisma`.
- **Tests**: Added `test-e2e.ts` and others in `scripts/`.

**Your Tasks:**

1.  **Code Review**:
    - Analyze `src/lib/superfaktura.ts` (especially `checkDuplicate` and `encodeSFSearch`).
    - Analyze `scripts/run-batch-test.ts` for error handling and logic.
    - Check `src/lib/logger.ts` and its usage in `src/app/api/process/route.ts`.
    - *Goal*: Identify any potential race conditions, unhandled edge cases, or type safety issues.

2.  **Bug Hunt**:
    - Run the existing tests: `npx tsx scripts/test-e2e.ts`.
    - Check `processing_summary.md` (generated by the last batch run) for any red flags.
    - Look for "TODO" or "FIXME" comments that might have been missed.

3.  **Documentation**:
    - Review `README.md`. Does it accurately reflect the *current* setup (including the new Logging feature and Duplicate logic)?
    - Verify setup instructions are complete.

**Deliverables:**
- A summary of findings (Bugs, logic gaps, documentation missing).
- Fixes for any critical bugs found.
- Updated `README.md` if necessary.

</instruction>